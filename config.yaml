runtime:
  workload: "GEMM"
  optimizer: "Adam"
  framework: "OpenCL"

optimizer:
  learning_rate: 0.001
  beta_1: 0.9
  beta_2: 0.999
  epsilon: 1e-08
  dim_m: 3024
  dim_k: 3024
  dim_n: 256
  batch_size: 256

workload:
  dataset_dir: "data/mnist"
  max_samples: 1024
  num_epochs: 5
